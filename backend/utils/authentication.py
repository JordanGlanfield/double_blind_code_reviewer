import subprocess



def update_password_file(username: str, password: str):
    pass
    # if "PASSWORD_FILE" in app.config:
    #     subprocess.run(["htpasswd", app.config["PASSWORD_FILE"], username, password])